include "application.conf"

# Dev overrides: bind localhost-only, disable security features for convenience
server {
    host = "127.0.0.1"
    host = ${?NYX_HOST}
}
rate_limit { enabled = false }
csrf      { enabled = false }
# storage.backend defaults to "local" in application.conf; no override needed
